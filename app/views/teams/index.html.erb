<div class="container">
  <h5>Team & Player Stats</h5>

  <!-- Loop through team names and urls by division for specefic team stats links -->
  <div class="team-section">
    <div class='blue-grey darken-1 btn'>Division A</div>
    <ul>
      <% @teams.where(division: "Premier").each do |t| %>
        <li><%= link_to "#{t.name}", "#{t.url}", target:'_blank', class:"link" %></li>
      <% end %>
    </ul>
  </div>
  <div class="team-section">
    <div class='blue-grey darken-1 btn'>Division B1</div>
    <ul>
      <% @teams.where(division: "B_East").each do |t| %>
        <li><%= link_to "#{t.name}", "#{t.url}", target:'_blank', class:"link" %></li>
      <% end %>
    </ul>
  </div>
  <div class="team-section">
    <div class='blue-grey darken-1 btn'>Division B2</div>
      <ul>
        <% @teams.where(division: "B_West").each do |t| %>
          <li><%= link_to "#{t.name}", "#{t.url}", target:'_blank', class:"link" %></li>
        <% end %>
      </ul>
  </div>
  <div class="team-section">
    <div class='blue-grey darken-1 btn'>Division C</div>
    <ul>
      <% @teams.where(division: "C").each do |t| %>
        <li><%= link_to "#{t.name}", "#{t.url}", target:'_blank', class:"link" %></li>
      <% end %>
    </ul>
  </div>
</div></br>
