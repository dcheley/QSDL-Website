<% if current_user %>
<div class="container">
  <div id="bars-top" class="section scrollspy">
    <h3>Manage Bar Info</h3>
  </div>

  <p><%= link_to "ADD BAR", new_bar_url %></p>

  <li class="divider"></li>

  <% @bars.each do |b| %>
    <li class="backend-list"><%= link_to "#{b.name}", "/bars/#{b.id}/edit" %></li>
    <p><b>Updated:</b> <%= b.updated_at.strftime("%a, %B %d, %Y") %></p>
    <%= button_to "Delete", {controller: :bars, action: 'destroy', id: b.id }, method: :delete %></br>
    <li class="divider"></li>
  <% end %>

  <div class="fixed-action-btn">
    <%= link_to "TOP", "#bars-top", class:"btn-floating btn-large blue-grey darken-3 white-text" %>
  </div>

<% else %>
<div class="container">
  <h3>Unauthorized!</h3>
<% end %>
</div></br>
<!--
Popper's Pub near Bathurst/bloor
add images paper clip
make news
STYLE FLASHES & LISTS
MAKE FIXED BUTTON FOR USER LINKS-->
