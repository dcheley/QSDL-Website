<% if current_user %>
<div class="container">
  <h3>Manage Bar Info</h3>

  <%= link_to "Add Bar", new_bar_url %>

  <% @bars.each do |b| %>
    <li><%= link_to "#{b.name}", "/bars/#{b.id}/edit" %> <%= button_to "Delete", {controller: :bars,
      action: 'destroy', id: b.id }, method: :delete %></li>
  <% end %>

<% else %>
<div class="container">
  <h3>Unauthorized!</h3>
<% end %>
</div>
<!--
Popper's Pub near Bathurst/bloor
add images paper clip
make news
STYLE FLASHES & LISTS
MAKE FIXED BUTTON FOR USER LINKS-->
